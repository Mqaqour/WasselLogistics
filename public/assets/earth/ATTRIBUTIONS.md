# Earth Texture Attribution

`earth-daymap.jpg` — NASA Visible Earth / Blue Marble land-ocean-ice imagery,
sourced via the three.js project's example assets
(`three.js/examples/textures/planets/earth_atmos_2048.jpg`, MIT-licensed repo).
NASA imagery is public domain (U.S. government work); no attribution is
legally required, but the source is credited here for traceability.

Used in `components/layout/HeroEarth.tsx` as a pure-CSS rotating backdrop —
no WebGL, no dependency on the 3D story section.
